# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# ELOHIM: standalone workspace root. This crate is a [patch.crates-io] target
# consumed by ../../Cargo.toml as a path dependency, not a workspace member.
# The empty table keeps cargo from attaching it to whatever workspace happens
# to sit above the checkout (in the elohim monorepo that is
# elohim/Cargo.toml, two levels up).
[workspace]

[package]
edition = "2024"
name = "kitsune2_transport_iroh"
version = "0.4.1"
authors = ["Holochain Core Dev Team <devcore@holochain.org>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The iroh-based transport implementation for kitsune2"
homepage = "https://www.holochain.org/"
readme = false
keywords = [
    "holochain",
    "kitsune",
    "p2p",
    "dht",
    "networking",
]
categories = ["network-programming"]
license = "Apache-2.0"
repository = "https://github.com/holochain/kitsune2"
resolver = "2"

[features]
metrics = [
    "dep:opentelemetry",
    "dep:opentelemetry_sdk",
]
schema = ["dep:schemars"]
test-utils = [
    "iroh-relay/test-utils",
    "dep:kitsune2_core",
    "dep:kitsune2_test_utils",
    "kitsune2_test_utils?/relay",
    "iroh/test-utils",
]

[lib]
name = "kitsune2_transport_iroh"
path = "src/lib.rs"

# ELOHIM: upstream's three integration-test targets (tests/integration.rs,
# tests/metrics.rs, tests/relay_auth.rs) are DECLARED OUT rather than deleted —
# the sources are kept verbatim for upstreaming reference. They need the
# kitsune2 workspace's dev-dependency set (axum, rustls, …) which the published
# tarball does not carry, so they cannot compile from a vendored copy, and they
# stand up live relay servers besides. `autotests = false` above means removing
# these blocks fully disables them; the unit tests in src/tests/ — including
# the cross-relay preflight coverage this patch adds — run under `cargo test
# --lib --features test-utils`.

# ELOHIM: the published tarball carries no [dev-dependencies], so the crate's
# own `#[cfg(test)]` modules (src/connection.rs uses `mockall::automock`)
# cannot compile from the registry copy. Restored here so the vendored patch
# is testable in place. Dev-dependencies do not enter the conductor's
# dependency graph — this is inert for the shipped binary.
[dev-dependencies]
mockall = "0.13"

[dependencies.base64]
version = "0.22.1"

[dependencies.bytes]
version = "1.8.0"
features = ["serde"]

[dependencies.iroh]
version = "0.95.1"
package = "iroh-holochain"

[dependencies.iroh-relay]
version = "0.95.1"
features = ["server"]
optional = true
package = "iroh-relay-holochain"

[dependencies.kitsune2_api]
version = "0.4.0-dev.0"

[dependencies.kitsune2_bootstrap_client]
version = "0.4.0-dev.0"

[dependencies.kitsune2_core]
version = "0.4.0-dev.0"
optional = true

[dependencies.kitsune2_test_utils]
version = "0.4.0-dev.0"
optional = true

[dependencies.n0-watcher]
version = "0.5"

[dependencies.opentelemetry]
version = "0.30"
optional = true

[dependencies.opentelemetry_sdk]
version = "0.30"
features = ["testing"]
optional = true

[dependencies.schemars]
version = "0.9"
features = ["preserve_order"]
optional = true

[dependencies.serde]
version = "1.0"
features = [
    "derive",
    "derive",
]

[dependencies.tokio]
version = "1.41.1"
features = [
    "macros",
    "rt",
    "rt-multi-thread",
    "sync",
    "time",
    "io-util",
]

[dependencies.tracing]
version = "0.1"

[dependencies.url]
version = "2.5.4"
